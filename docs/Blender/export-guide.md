# Export Guide

Run `bash Scripts/export-models.sh` when Blender CLI is installed.

Exports should be binary `.glb`, include materials, apply transforms and exclude reference images. Generated files are ignored until reviewed.
