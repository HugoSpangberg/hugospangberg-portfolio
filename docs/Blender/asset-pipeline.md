# Blender Asset Pipeline

Career-world landmark geometry is moving from procedural Three.js primitives toward authored GLB assets.

Current state:

- Blender scripts exist in `Assets/Blender/Scripts`.
- Source `.blend`, exported `.glb`, previews and reports are intentionally ignored until generated locally.
- `HSClient/public/models/career-world/manifest.json` declares the intended runtime assets.
- Procedural Three.js landmarks remain the runtime fallback.

Run:

```bash
bash Scripts/export-models.sh
npm run models:validate
```

Blender CLI was not available during this pass, so no `.blend` or `.glb` files were generated or verified.
# Blender Career World Asset Pipeline

Blender is used for authored miniature geometry: buildings, terrain, forestry props, bevels, normals, materials and optional animation clips. Plain Three.js remains the runtime because the portfolio already uses custom controls, raycasting, HTML overlays, aurora, data pulses, fallback behavior and CMS-driven localized content.

## Ownership

- Blender owns source geometry and PBR material values.
- `HSClient/public/models/career-world` owns runtime GLB delivery.
- Three.js owns renderer setup, camera, controls, lights, interaction, labels, cards, atmosphere, animation lifecycle and fallback.

## Commands

```bash
npm run models:export
npm run models:preview
npm run models:validate
```

Set `BLENDER_EXECUTABLE` only when Blender is not on PATH or in a standard install location.

## Fallback

The procedural world remains a functional fallback. The GLB environment hides the procedural base only after it validates. Each landmark hides its procedural counterpart only after its own GLB validates. This keeps the hero interactive during local asset work and partial deployment failures.

## Current Limitations

The models are stylized low-poly miniatures generated by Blender Python. They are intended for the hero camera, not close architectural inspection. Future manual Blender polish should focus on facade detail, signage typography and custom UV-free material variation.
