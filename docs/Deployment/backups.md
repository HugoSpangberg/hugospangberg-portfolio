# Backups

Back up independently:

- `HSPortfolioCms`
- HSCms media storage
- `HSPortfolioApi`

CMS backups protect editorial history and media. API backups protect last-known-good snapshots and short-lived operational greeting state.

Greeting data should be retained only as long as needed for idempotency, abuse protection and diagnostics.
